import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color primary = Color(0xFF2D5F7C);
  static const Color primaryLight = Color(0xFF527DA1);
  static const Color primaryDark = Color(0xFF1B3B4E);

  // Secondary colors
  static const Color secondary = Color(0xFF48B2A0);
  static const Color secondaryLight = Color(0xFF6BC7B8);
  static const Color secondaryDark = Color(0xFF348F80);

  // Accent colors
  static const Color accent = Color(0xFFFFA726);
  static const Color accentLight = Color(0xFFFFB851);
  static const Color accentDark = Color(0xFFE69220);

  // Background colors
  static const Color background = Color(0xFFF5F7FA);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color card = Color(0xFFFFFFFF);

  // Text colors
  static const Color textPrimary = Color(0xFF2D3436);
  static const Color textSecondary = Color(0xFF636E72);
  static const Color textLight = Color(0xFFB2BEC3);

  // Status colors
  static const Color success = Color(0xFF00B894);
  static const Color error = Color(0xFFFF7675);
  static const Color warning = Color(0xFFFFBE76);
  static const Color info = Color(0xFF74B9FF);

  // Border and divider colors
  static const Color border = Color(0xFFE5E9F2);
  static const Color divider = Color(0xFFEDF2F7);

  // Shimmer colors for loading states
  static const Color shimmerBase = Color(0xFFE5E9F2);
  static const Color shimmerHighlight = Color(0xFFF5F7FA);
}
